# Port where n8n will be exposed
N8N_PORT=5678

N8N_SECURE_COOKIE=false

# Basic Auth (recommended for public or shared deployments)
BASIC_AUTH_ACTIVE=true
BASIC_AUTH_USER=admin
BASIC_AUTH_PASSWORD=changeme123

# Timezone (e.g., Europe/Berlin, America/New_York)
TIMEZONE=UTC

# External PostgreSQL database configuration
DB_HOST=your-postgres-host
DB_PORT=5432
DB_NAME=n8n
DB_USER=n8n
DB_PASSWORD=your-db-password
